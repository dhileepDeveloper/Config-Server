eureka:
  client:
    service-url:
     defaultZone: http://${other.eureka.hostname}:${other.eureka.port}/eureka
  server:
    enable-self-preservation: false
  instance:
    hostname: localhost

server:
 port: 8761

other:
 eureka:
  hostname: localhost
  port: 8762


  

 
